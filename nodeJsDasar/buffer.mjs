const buffer = Buffer.from("andi matulada")

// const buffer = Buffer.from("andi matulada")
// console.log(buffer); Representasi dr string "andi matulada"

console.log(buffer);
console.log(buffer.toString());

buffer.reverse()
console.log(buffer.toString());